#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define inf 0x3f3f3f
#define mem(a,b)  memset( a,b,sizeof a)
int main()
{
    std::ios::sync_with_stdio(false);
    int T;
    while(cin>>T)
    {
        mem(f[])
        while(T--)
        {
            int l,r;
            char c;
          cin>>l>>r>>c;
          if(c=='w')
          {

          }
          else
          {

          }
        }
    }
    return 0;
}
