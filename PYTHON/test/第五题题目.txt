程序代码的相似度判定程序。编程实现对D盘test目录下的子目录之间的同名文件进行比较，比较前去除文件中的python关键字、Built-in Functions名和Built-in Constants名、忽略掉所有的import语句、将所有的运算符号及括号以空格代替，最后将所有连续空格和跳格用一个空格代替，这样基本保留下文件中的变量名，以文件中的变量名的相似情况来比较，输出相似度最高的前三个目录名。