#include<iostream>
#include<cstring>
using namespace std;
int main()
{
    int T;
    cin>>T;
    int f[10000];
    int k[10000];
    while(T--)
    {
    	memset(f,0,sizeof(f));
    	memset(k,0,sizeof(k));
    	int n,m,k,p;
    	cin>>n>>m>>k>>p;
    	for(int i=1;i<=n;i++)
    		f[i]++;
	}
    
    return 0;
}
