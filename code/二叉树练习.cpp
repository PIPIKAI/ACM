#include<iostream>
#include<stdio.h>
#include<cstring>
using namespace std;
string a,b,c;
void solve(string a,string b)
{
    c.push(a[0]);
    for(int i=0;i<a.size();i++)
        if(a[])
}
int main()
{
    cin>>a>>b;
    solve(s,b);
    cout<<c<<end;
    return 0;
}
