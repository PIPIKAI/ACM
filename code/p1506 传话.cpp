/*
作者:kaiss
题目:p1506 传话
*/
#include <stdio.h>
int beback(int i,int **f)
{
    int j;
    for(i=0;i<a;i++)
    for()
        if(f[i][1]==a+1)
}
int beup(int i,int **f)
int main()
{
    int a, b,i,j,flag;
    scanf("%d%d", &a, &b);
    int f[b][2],b[100];
    for(i=0;i<b;i++)
    {
        scanf("%d%d",&f[i][0],&f[i][1]);
    }
    for(i=0;i<a;i++)
    {

        if(flag==0)
            printf("F\n");
    }

    return 0;
}
