#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef struct {
    int d,w;
}IN;
#define INF 0X7777777
IN f[3030];
int k[4][3005];
int main()
{
    memset(k,INF,sizeof(k));
    int n;
    for(int i=n;i--;)
        scanf("%d",&f[i].d)
    for(int i=n;i--;)
        scanf("%d",&f[i].w)

    for(int i=0;i<3;i++)
    {
        for(int j=0;j<n;j++)
        {
            k[i][j]=
        }
    }
    return 0;
}

